<h1>
	<%= review.username %>'s
	</br>
	<%= review.projectName %>
</h1>
<form method="POST" action="/review" onsubmit="fill()" id="form">
	<script>
		'<% let taskNumber = 0 %>';
		'<% for (let i = 0; i < components.length; i++){ %>'
			var node = document.createElement('H2');
			var textnode = document.createTextNode('<%= components[i].component %>');
			node.appendChild(textnode);
			document.getElementById("form").appendChild(node);
			'<% for (let j = 0; j < components[i].tasks.length; j++){ %>'
				var node2 = document.createElement('null');
				var textnode2 = document.createTextNode('<%= components[i].tasks[j] %>');
				node2.appendChild(textnode2);
				document.getElementById("form").appendChild(node2);
				var node3 = document.createElement('INPUT');
				'<% if(review.completion > taskNumber){ %>'
					node3.type = 'hidden';
				'<% } else { %>'
					node3.type = 'checkbox';
				'<% } %>'
				node3.name = `task${'<%= taskNumber++ %>'}`;
				document.getElementById("form").appendChild(node3);
				document.getElementById("form").appendChild(document.createElement('BR'));
			'<% } %>'
		'<% } %>'
		document.getElementById("form").appendChild(document.createElement('BR'));
	</script>
	Comment</br>
	<input type="text" name="comment" required></br>
	<input type="hidden" id="usr" name="username">
	<input type="hidden" id="prj" name="project">
	<input type="submit" value="submit">
</form>
<script>
	function fill(){
		const username = '<%= review.username %>';
		const project = '<%= review.projectName %>';
			document.getElementById("usr").value = username;
			document.getElementById("prj").value = project;
	};
</script>